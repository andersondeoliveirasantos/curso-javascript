const phrase = 'It has number 100 is here'.match(/\d+/) // Tem o número 100 aqui
const phrase2 = 'Have the number is here'.match(/\d+/) // Tem o número está aqui
 
console.log(phrase)
console.log(phrase2)